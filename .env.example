# The .NET backend reads config from backend/src/DonkeyWork.A2AExplorer.Api/appsettings.Development.json
# (gitignored — copy from appsettings.json and fill in secrets). There are no frontend-only env vars.
